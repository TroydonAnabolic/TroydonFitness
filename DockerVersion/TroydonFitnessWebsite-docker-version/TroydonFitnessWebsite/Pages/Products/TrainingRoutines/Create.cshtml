@page
@model TroydonFitnessWebsite.Pages.Products.TrainingRoutines.CreateModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Pages/Shared/_ProductsLayout.cshtml";
}

<h1>Create</h1>

@*/ Things to display using values from drop down list: title, price, quantity, available, last updated, length of routine, Description, Session type, experience level*@

<h4>TrainingRoutine</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TrainingRoutineVM.RoutineName" class="control-label"></label>
                <select asp-for="@Model.getIndexOfPt" class="form-control"
                        asp-items="@Model.PtNameSL">
                    <option value="">-- Select PT Name--</option>
                </select>
                <span asp-validation-for="TrainingRoutineVM.RoutineName" class="text-danger" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
