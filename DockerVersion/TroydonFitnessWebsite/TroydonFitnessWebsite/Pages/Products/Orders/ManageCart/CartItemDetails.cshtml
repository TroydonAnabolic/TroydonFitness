@page
@model TroydonFitnessWebsite.Pages.Products.Orders.ManageCart.CartItemDetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div>
    <h4>Cart Item Detail</h4>
    <hr />

    <dl class="row">

        <dt class="col-sm-2">
            @if (Model.Cart.TrainingRoutine != null)
            {
                @Html.DisplayNameFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.Product.ProductID)
            }
            else if (Model.Cart.Supplement != null)
            {
                @Html.DisplayNameFor(model => model.Cart.Supplement.SupplementID)
            }
        </dt>
        <dd class="col-sm-10">
            @if (Model.Cart.TrainingRoutine != null)
            {
                @Html.DisplayFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.Product.ProductID)
            }
            else if (Model.Cart.Supplement != null)
            {
                @Html.DisplayFor(model => model.Cart.Supplement.SupplementID)
            }
        </dd>
        <dt class="col-sm-2">
            @if (Model.Cart.TrainingRoutine != null)
            {
                @Html.DisplayNameFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.Product.Title)
            }
            else if (Model.Cart.Supplement != null)
            {
                @Html.DisplayNameFor(model => model.Cart.Supplement.Product.Title)
            }
        </dt>
        <dd class="col-sm-10">
            @if (Model.Cart.TrainingRoutine != null)
            {
                @Html.DisplayFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.Product.Title)
            }
            else if (Model.Cart.Supplement != null)
            {
                @Html.DisplayFor(model => model.Cart.Supplement.Product.Title)
            }
        </dd>
        <dt class="col-sm-2">
            @if (Model.Cart.TrainingRoutine != null)
            {
                @Html.DisplayNameFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.PtProductPicture)

            }
            else if (Model.Cart.Supplement != null)
            {
                @*<img style="width:7rem;height:7rem; object-fit:cover;" src="data:image/*;base64,@(Convert.ToBase64String(Model.Cart.Supplement.SupplementPicture))">*@
            }
        </dt>
        <dd class="col-sm-10">
            @if (Model.Cart.TrainingRoutine != null) // TODO: Allow reset of cooldown date on user account when user removes cart item from cart
            {
                <img style="width:7rem;height:7rem; object-fit:cover;" src="data:image/*;base64,@(Convert.ToBase64String(Model.Cart.TrainingRoutine.PersonalTrainingSession.PtProductPicture))">
            }
            else if (Model.Cart.Supplement != null)
            {
                @*<img style="width:7rem;height:7rem; object-fit:cover;" src="data:image/*;base64,@(Convert.ToBase64String(Model.Cart.Supplement.SupplementPicture))">*@
            }
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.Product.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Cart.TrainingRoutine.PersonalTrainingSession.Product.Price)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Cart.Quantity)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Cart.Quantity)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Cart.CartID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
